apply from: "gradle/vertx.gradle"

dependencies {
    compile "org.mongodb:mongo-java-driver:$mongoDbVersion"
    compile "org.imgscalr:imgscalr-lib:$imgscalrVersion"
}

javadoc {
  options.encoding = 'UTF-8'
  classpath = configurations.provided
  classpath += configurations.compile
}

def configurePom(def pom) {
	pom.project {
    name 'mod-image-resizer'
		description 'Image resizer BusMod for Vert.x'
		inceptionYear '2013'
		packaging 'zip'
		url 'https://github.com/web-education/mod-image-resizer'

		developers {
			developer {
				id 'dboissin'
				name 'Damien BOISSIN'
				email 'damien.boissin@web-education.net'
			}
		}

		scm {
      connection 'scm:git:ssh://git@github.com:web-education/mod-image-resizer.git'
      developerConnection 'scm:git:ssh://git@github.com:web-education/mod-image-resizer.git'
      url 'https://github.com/web-education/mod-image-resizer.git'
		}

		licenses {
			license {
				name 'The Apache Software License, Version 2.0'
				url 'http://www.apache.org/licenses/LICENSE-2.0.txt'
				distribution 'repo'
			}
		}

		properties {
			setProperty('project.build.sourceEncoding', 'UTF8')
		}
	}
}

